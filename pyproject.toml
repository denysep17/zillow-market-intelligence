[project]
name = "zillow-market-intelligence"
version = "0.1.0"
description = "Early-warning housing market intelligence and forecasting platform using public Zillow Research data."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
